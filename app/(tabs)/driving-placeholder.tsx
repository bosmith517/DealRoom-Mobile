/**
 * Driving Placeholder
 *
 * This screen is never shown - the FAB navigates to /driving instead.
 * Required by expo-router to have a file for the tab name.
 */

import { View } from 'react-native'

export default function DrivingPlaceholder() {
  return <View />
}
